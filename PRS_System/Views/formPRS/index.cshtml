@model PRS_System.Models.FormModel.IndexListFormModel
@using Microsoft.AspNetCore.Http
@using PRS_System.Models.Data
@using PRS_System.Controllers
@{
    Layout = "_BlankLayout";
}
<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!-->
<html class="no-js">
<!--<![endif]-->
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Home</title>
    <meta name="description" content="">
    <!--
    Volton Template
    http://www.templatemo.com/tm-441-volton
    -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link href='https://fonts.googleapis.com/css?family=Kanit&subset=thai,latin' rel='stylesheet' type='text/css'>

    <link rel="stylesheet" href="~/css/normalize.css">
    <link rel="stylesheet" href="~/css/font-awesome.css">
    <link rel="stylesheet" href="~/css/bootstrap.min.css">
    <link rel="stylesheet" href="~/css/templatemo-style.css">
    <link rel="stylesheet" href="css/table.css">
    <script src="js/vendor/modernizr-2.6.2.min.js"></script>
    @*<link rel="stylesheet" href="~/js/IndexForm.js" />*@
    <script src="js/vendor/jquery-1.10.2.min.js"></script>
    <script src="js/min/plugins.min.js"></script>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="sweetalert2.min.js"></script>
    <link rel="stylesheet" href="sweetalert2.min.css">
    <style>

        .dropdown {
            padding: 15px 40px;
            display: block;
            font-size: 14px;
        }

            .dropdown i {
                margin-right: 15px;
                font-size: 24px;
            }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #EEEEEE;
            min-width: 160px;
            border-radius: 5px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }

            .dropdown-content a {
                padding: 12px 16px;
                text-decoration: none;
                display: block;
            }

            .dropdown-content:hover a:hover {
                background-color: #dddddd;
                border-radius: 5px;
            }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .dropdown:hover a {
            color: #5f6d7e;
        }

        .banner-bg {
            padding: 260px 80px;
            position: relative;
            background: url('~/img/login/image_science.jpg');
            background-repeat: no-repeat;
            background-position: center;
            background-size: cover;
            z-index: 2;
        }

        .banner-overlay {
            position: absolute;
            width: 100%;
            height: 100%;
            background: rgba($dark-bg, 0.4);
            top: 0;
            left: 0;
            z-index: 3;
        }
    </style>
</head>
<body>
    <!--[if lt IE 7]>
        <p class="browsehappy">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
    <![endif]-->

    <div class="responsive-header visible-xs visible-sm">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="top-section">
                        <div class="profile-content">
                            <h3 class="profile-title">@Context.Session.GetString("uid")</h3>
                            <p class="profile-description">@Context.Session.GetString("thainame")</p>
                        </div>
                    </div>
                </div>
            </div>
            <a href="#" class="toggle-menu"><i class="fa fa-bars"></i></a>
            <div class="main-navigation responsive-menu">
                <ul class="navigation">
                    @*<li><a href="#home"><i class="fa fa-globe"></i>Home</a></li>
                    <li><a href="#history"><i class="fa fa-pencil"></i>History</a></li>
                    <li><a href="#form"><i class="fa fa-link"></i>Form</a></li>*@
                    <li><a href="#setting"><i class="fa fa-cog"></i>Admin Setting</a></li>
                </ul>
            </div>
        </div>
    </div>

    <!-- SIDEBAR -->
    <div class="sidebar-menu hidden-xs hidden-sm">
        <div class="top-section">
            <h3 class="profile-title">@Context.Session.GetString("uid")</h3>
            <p class="profile-description">@Context.Session.GetString("thainame")</p>
        </div> <!-- top-section -->
        <div class="main-navigation">
            <ul class="navigation">
                @*<li><a href="#home"><i class="fa fa-globe"></i>Home</a></li>
                <li><a href="#history"><i class="fa fa-pencil"></i>History</a></li>
                <li><a href="#form"><i class="fa fa-link"></i>Form</a></li>*@
                @if (Context.Session.GetString("type_person").ToString() == "Admin")
                {
                    <li class="dropdown">
                    <i class="fa fa-cog"></i>Admin Setting
                    <div class="dropdown-content">
                        <a href="@Url.Action("ShowListuser","AdminSetting")" id="Admin_Setting">Admin Control</a>
                        <a href="@Url.Action("informationsetting","AdminSetting")" id="Information_Setting">Information Control</a>
                    </div>
                </li>
                }
                
                @if (!string.IsNullOrEmpty(Context.Session.GetString("AccessToken")))
                {
                    <li><a href="@Url.Action("Logout","Login")"><i class="fa fa-sign-out"></i>Logout</a></li>
                }
            </ul>
        </div> <!-- .main-navigation -->
    </div> <!-- .sidebar-menu -->

    <!-- MAIN CONTENT -->
    <div class="main-content">
        <div class="fluid-container">

            <div class="w3-container w3-card-4 w3-light-grey">

                <!-- HISTORY -->
                <div class="page-section" id="history">
                    <div class="row">
                        <div class="col-md-12">
                            <h4 class="widget-title">History</h4>
                            <p>ตารางหน้าคำขอผู้จัดซื้อ</p>
                            <button class="button button1" id="createbutton">+Create</button>

                            @*keyword*@
                            <br><label>Search</label>
                            <form asp-action="index" asp-controller="FormPRS" method="post">
                                <input class="w3-input w3-border w3-round" type="text" asp-for="Keyword" placeholder="search...">
                                <button id="" class="button button4" type ="submit">ค้นหา</button>
                            </form>
                            
                            <hr>
                            <table id="customers">
                                <tr>
                                    <th>No.</th>
                                    <th>Procurement Request</th>
                                    <th>Status</th>
                                    <th>
                                        waiting
                                    </th>
                                    <th>Function</th>
                                    <th>Date</th>
                                </tr>
                                @{int countfield = Model.ListForm != null ? Model.ListForm.Count : 0;}
                                @if(countfield ==0)
                                {
                                    <tr>
                                        <td colspan="6">
                                            ไม่พบข้อมูลใบคำร้องจัดซื้อหรือจ้าง
                                        </td>
                                    </tr>
                                }
                                @for (int i = 0; i < countfield; i++)
                                {
                                    int number = i + 1;
                                    FormPRSDataModel item = Model.ListForm[i];
                                    <tr>

                                        <td>@number</td>
                                        <td>@item.nameProcument</td>
                                        @{
                                            if (item.Status == "Sent")
                                            {
                                                <td style="color:blue">@item.Status</td>
                                            }
                                            else if (item.Status == "Sent to Approval")
                                            {
                                                <td style="color:cornflowerblue">@item.Status</td>
                                            }
                                            else if (item.Status == "Return to Requester")
                                            {
                                                <td style="color:red">@item.Status</td>
                                            }
                                            else if (item.Status == "Approved")
                                            {
                                                <td style="color:green">@item.Status</td>
                                            }
                                            else if (item.Status == "Draft")
                                            {
                                                <td style="color:black">@item.Status</td>
                                            }
                                        }
                                        <td>
                                            @item.cerrent_flow
                                        </td>

                                        <td>
                                            <button style="display:@(item.Status=="Return to Requester"?"None":"block" )" class="button button2" onclick="formpage(@item.id_tor)">View</button>
                                            <button type="button" style="display:@(item.Status=="Return to Requester"?"block":"None" )" class="button button2" onclick="formpage(@item.id_tor)">Edit</button>
                                            <button type="button" style="display:@(item.Status=="Return to Requester"?"block":"None" )" onclick="deleteAllFormPRS(@item.id_tor)" class="button button3">Delete</button>
                                            <button class="button button1" style="display:@(item.Status=="Approved"?"block":"None" )" type="button" onclick="torpdf(@item.id_tor)">Export TOR PDF</button>
                                            <button class="button button4" style="display:@(item.Status=="Approved"?"block":"None" )" type="button" onclick="productpdf(@item.id_tor)">Export PRODUCT PDF</button>
                                            @*<button type="button" style="display:@(item.Status=="Return to Requester"?"block":"None" )" class="button button4">Draft</button>*@
                                        </td>
                                        <td>@item.Date.Value.ToString("dd-MM-yyyy")</td>
                                    </tr>
                                }
                            </table>
                            <br>
                            <div style="display:@(Model.category_user=="Admin"?"block":"None" )">

                                <p>ตารางหน้าฝ่ายพัสดุ</p>
                                <hr>
                                @*<form asp-action="index" asp-controller="FormPRS" method="post">
                                    <input class="w3-input w3-border w3-round" type="text" asp-for="Keyword2" placeholder="search...">
                                    <button id="" class="button button4" type="submit">ค้นหา</button>
                                </form>*@
                            <table id="customers" style="display:@(Model.category_user=="Admin"?"block":"None" )">
                                <tr>
                                    <th>No.</th>
                                    <th>Name of Purchaser</th>
                                    <th>Procurement Request</th>
                                    <th>Status</th>
                                    <th>
                                        waiting
                                    </th>
                                    <th>Function</th>
                                    <th>Date</th>
                                </tr>

                                @{int countfield2 = Model.ListSuppies != null ? Model.ListSuppies.Count : 0;}
                                @if (countfield2 == 0)
                                {
                                    <tr>
                                        <td colspan="7">
                                            ไม่พบข้อมูลใบคำร้องจัดซื้อหรือจ้าง
                                        </td>
                                    </tr>
                                }
                                @{

                                    @for (int i = 0; i < countfield2; i++)
                                    {
                                        int number2 = i + 1;
                                        FormPRSDataModel item = Model.ListSuppies[i];
                                        if (item.Status != "Return to Requester" && item.Status != "Draft")
                                        {
                                            <tr>
                                                <td>@number2</td>
                                                <td>@item.Fullname_PRS</td>
                                                <td>@item.nameProcument</td>
                                                @{
                                                    if (item.Status == "Sent")
                                                    {
                                                        <td style="color:blue">@item.Status</td>
                                                    }
                                                    else if (item.Status == "Sent to Approval")
                                                    {
                                                        <td style="color:cornflowerblue">@item.Status</td>
                                                    }
                                                    else if (item.Status == "Return to Requester")
                                                    {
                                                        <td style="color:red">@item.Status</td>
                                                    }
                                                    else if (item.Status == "Approved")
                                                    {
                                                        <td style="color:green">@item.Status</td>
                                                    }
                                                    else if (item.Status == "Draft")
                                                    {
                                                        <td style="color:black">@item.Status</td>
                                                    }
                                                }
                                                <td>
                                                    @item.cerrent_flow
                                                </td>
                                                <td>
                                                    <button class="button button2" onclick="formpage(@item.id_tor)">View And approval</button>
                                                    <button type="button" style="display:@(item.Status=="Return to Requester" || item.Status=="Sent"?"block":"None" )" onclick="deleteAllFormPRS(@item.id_tor)" class="button button3">Delete</button>
                                                    <button class="button button1" style="display:@(item.Status=="Sent"?"block":"None" )" type="button" onclick="torpdf(@item.id_tor)">Export TOR PDF</button>
                                                    <button class="button button4" style="display:@(item.Status=="Sent"?"block":"None" )" type="button" onclick="productpdf(@item.id_tor)">Export PRODUCT PDF</button>
                                                </td>
                                                <td>@item.Date.Value.ToString("dd-MM-yyyy")</td>
                                            </tr>

                                        }


                                    }
                                }






                            </table>
                            </div>
                            <div style="display:@(Model.magnage_pos==""?"None":"block" )">
                                <p>ตารางหน้าผู้อนุมัติ</p>
                                <hr>
                                @*<form asp-action="index" asp-controller="FormPRS" method="post">
                                    <input class="w3-input w3-border w3-round" type="text" asp-for="Keyword3" placeholder="search...">
                                    <button id="" class="button button4" type="submit">ค้นหา</button>
                                </form>*@
                            <table id="customers" style="display:@(Model.magnage_pos==""?"none":"block" )">
                                <tr>
                                    <th>No.</th>
                                    <th>Name of Purchaser</th>
                                    <th>Procurement Request</th>
                                    <th>Status</th>
                                    <th>
                                        waiting
                                    </th>
                                    <th>Function</th>
                                    <th>Date</th>
                                </tr>

                                @{int countfield3 = Model.ListApproval.Count;}
                                @if (countfield3 == 0)
                                {
                                    <tr>
                                        <td colspan="7">
                                            ไม่พบข้อมูลใบคำร้องจัดซื้อหรือจ้าง
                                        </td>
                                    </tr>
                                }
                                @{
                                    @for (int i = 0; i < countfield3; i++)
                                    {
                                        int number3 = i + 1;
                                        FormPRSDataModel item = Model.ListApproval[i];
                                        if (item.Status != "Return to Requester" && item.Status != "Draft" && item.Status != "Sent")
                                        {
                                            <tr>
                                                <td>@number3</td>
                                                <td>@item.Fullname_PRS</td>
                                                <td>@item.nameProcument</td>
                                                @if (item.Status == "Sent")
                                                {
                                                    <td style="color:blue">@item.Status</td>
                                                }
                                                else if (item.Status == "Sent to Approval")
                                                {
                                                    <td style="color:cornflowerblue">@item.Status</td>
                                                }
                                                else if (item.Status == "Return to Requester")
                                                {
                                                    <td style="color:red">@item.Status</td>
                                                }
                                                else if (item.Status == "Approved")
                                                {
                                                    <td style="color:green">@item.Status</td>
                                                }
                                                else if (item.Status == "Draft")
                                                {
                                                    <td style="color:black">@item.Status</td>
                                                }
                                                <td>
                                                    @item.cerrent_flow
                                                </td>
                                                <td>
                                                    <button class="button button2" onclick="formpage(@item.id_tor)">View And approval</button>
                                                </td>
                                                <td>@item.Date.Value.ToString("dd-MM-yyyy")</td>
                                            </tr>

                                        }


                                    }
                                }






                            </table>
                            </div>
                            <br>
                            
                        </div>
                    </div> <!-- #history -->
                </div>

                <div class="row" id="footer">
                    @*<div class="col-md-12 text-center">
                        <p class="copyright-text">Faculty Science &copy; Kasatsart University Sriracha campas</p>
                        </div>*@
                </div>

            </div>

        </div>
    </div>



</body>
</html>
<script>
    $(document).ready(function () {
        $("#createbutton").click(function () {
            location.href = "@Url.Action("form", "FormPRS")"+"?id_tor=0";
        });
        
        $('#UserSetting').click(function () {
            location.href = "@Url.Action("ShowListuser","AdminSetting")";
        });
        
});
</script>
<script>
    function formpage(id_tor) {
           location.href = "@Url.Action("form", "FormPRS")"+"?id_tor="+id_tor;
    }
    function torpdf(id_tor) {
        location.href = "@Url.Action("torpdf", "FormPRS")"+"?id_tor="+id_tor;
    }
    function productpdf(id_tor) {
        location.href = "@Url.Action("productpdf", "FormPRS")"+"?id_tor="+id_tor;
    }
    function deleteAllFormPRS(id_tor) {
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                var button = $(this);
                var url = "@Url.Action("DeleteFormTOR", "FormPRS")" + "?id_tor=" + id_tor
                $.ajax({
                    type: "POST",
                    url: url,
                    data: button.serialize(), // serializes the form's elements.  //ดึงข้อมุลในฟอร์ม
                    dataType: "JSON",
                    success: function (data) {
                        console.log(data);
                        if (data.status == "success") {
                            Swal.fire({
                                icon: 'success',
                                title: data.messege,
                                showConfirmButton: true,
                            }).then((result2) => {
                                if (result2.isConfirmed) {
                                    location.reload();
                                }
                            });
                        }
                        else if (data.status == "error") {
                            Swal.fire({
                                icon: 'error',
                                title: data.errorMessage,
                                text: data.detail,
                                showConfirmButton: true,
                            });
                        }
                    },
                    error: function (err) {
                        console.log("Error", err);
                        alert("Error save fail");
                    }
                });
            }
        });
    }
</script>